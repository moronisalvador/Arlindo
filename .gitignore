node_modules
dist
dist-ssr
*.local
.env.local
.env.*.local
.DS_Store
coverage
dev-dist
*.log
.vite

# Local slide screenshots (npm run shot) + Playwright browser cache
screenshots/
test-results/
playwright-report/

# Client illustration samples (contain PII) — keep local, never commit
Dave Miller.pdf
/samples/
*.illustration.pdf
# Belt-and-suspenders: no client illustration PDF (any name) is ever committed
*.pdf
